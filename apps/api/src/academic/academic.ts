import { BadRequestException, Body, Controller, Get, Injectable, Param, Post, Query, UseGuards } from '@nestjs/common';
import { EducationModality, Role } from '@prisma/client'; import { IsInt, IsOptional, IsString } from 'class-validator';
import { PrismaService } from '../prisma.service'; import { JwtAuthGuard,Roles,RolesGuard } from '../common/security';
class SectionDto{ @IsString() academicYearId!:string; @IsString() studyPlanId!:string; @IsInt() gradeLevel!:number; @IsString() name!:string; @IsOptional() @IsString() shift?:string; @IsOptional() @IsInt() capacity?:number; }
@Injectable() export class AcademicService{constructor(private db:PrismaService){} years(){return this.db.academicYear.findMany({orderBy:{startDate:'desc'},include:{gradingPolicy:true}})} plans(){return this.db.studyPlan.findMany({include:{subjects:{include:{subject:true},orderBy:[{gradeLevel:'asc'},{sortOrder:'asc'}]}}})} sections(year?:string){return this.db.section.findMany({where:{academicYearId:year},include:{academicYear:true,studyPlan:true},orderBy:[{gradeLevel:'asc'},{name:'asc'}]})} createSection(d:SectionDto){return this.db.section.create({data:d})} geography(){return this.db.federalState.findMany({include:{municipalities:{include:{parishes:true},orderBy:{name:'asc'}}},orderBy:{name:'asc'}})} }
@UseGuards(JwtAuthGuard,RolesGuard) @Controller('academic') export class AcademicController{constructor(private s:AcademicService){} @Get('years') years(){return this.s.years()} @Get('plans') plans(){return this.s.plans()} @Get('sections') sections(@Query('academicYearId')year?:string){return this.s.sections(year)} @Roles(Role.ADMIN,Role.DIRECTOR) @Post('sections') section(@Body()d:SectionDto){return this.s.createSection(d)} @Get('geography') geo(){return this.s.geography()} }
