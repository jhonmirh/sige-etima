import { Body, Controller, Get, Injectable, Param, Post, UseGuards } from '@nestjs/common'; import { PrismaService } from '../prisma.service'; import { JwtAuthGuard,Roles,RolesGuard } from '../common/security'; import { Role } from '@prisma/client';
@Injectable() export class StaffService{constructor(private db:PrismaService){} list(){return this.db.staff.findMany({include:{qualifications:true},orderBy:{lastName:'asc'}})} create(data:any){const d={...data}; if(d.birthDate)d.birthDate=new Date(d.birthDate); if(d.ministryEntryDate)d.ministryEntryDate=new Date(d.ministryEntryDate); return this.db.staff.create({data:d,include:{qualifications:true}})} get(id:string){return this.db.staff.findUniqueOrThrow({where:{id},include:{qualifications:true,assignments:{include:{section:true,studyPlanSubject:{include:{subject:true}}}}}})} }
@UseGuards(JwtAuthGuard,RolesGuard) @Controller('staff') export class StaffController{constructor(private s:StaffService){} @Get() list(){return this.s.list()} @Get(':id') get(@Param('id')id:string){return this.s.get(id)} @Roles(Role.ADMIN,Role.DIRECTOR,Role.SECRETARIA) @Post() create(@Body()d:any){return this.s.create(d)} }
