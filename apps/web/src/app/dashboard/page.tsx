'use client'; import {useEffect,useState} from 'react'; import Shell from '@/components/Shell'; import {api} from '@/lib/api';
export default function Dashboard(){const [d,setD]=useState<any>(null);useEffect(()=>{api('/reports/dashboard').then(setD).catch(()=>{})},[]);const cards=[['Inscritos',d?.total??'—'],['Retiros',d?.withdrawn??'—'],['Representantes',d?.representatives??'—'],['Aportes',d?`${d.contributions.toLocaleString('es-VE')} Bs`:'—']];return <Shell title="Panel institucional"><div className="grid">{cards.map(x=><div className="card" key={x[0]}><div className="muted">{x[0]}</div><div className="metric">{x[1]}</div><div className="muted">Año {d?.year?.name||'vigente'}</div></div>)}</div><div className="section-title"><h2>Inscritos por grado</h2></div><div className="card bars">{d?.byGrade?.map((g:any)=><div className="bar" key={g.gradeLevel}><b>{g.gradeLevel}° año</b><div className="track"><div className="fill" style={{width:`${Math.min(100,(g._count/(d.total||1))*100)}%`}}/></div><span>{g._count}</span></div>)||<span className="muted">Sin datos todavía.</span>}</div></Shell>}
