'use client'; import {useEffect,useState} from 'react'; import Shell from '@/components/Shell'; import {api} from '@/lib/api';
export default function Plans(){const [rows,setRows]=useState<any[]>([]);useEffect(()=>{api('/academic/plans').then(setRows)},[]);return <Shell title="Planes de estudio"><div className="grid">{rows.map(p=><div className="card" key={p.id}><div className="muted">Plan {p.code}</div><h2>{p.name}</h2><p>{p.modality} · 1° a {p.maxGrade}°</p><p className="muted">Título: {p.titleName}</p><b>{p.subjects.length} asignaciones curriculares cargadas</b></div>)}</div><div className="section-title"><h2>Mallas de referencia</h2></div><div className="grid" style={{gridTemplateColumns:'1fr 1fr'}}><div className="card"><img src="/brand/plan-31059.png" style={{width:'100%',maxHeight:520,objectFit:'contain'}}/></div><div className="card"><img src="/brand/plan-41049.png" style={{width:'100%',maxHeight:520,objectFit:'contain'}}/></div></div></Shell>}
