'use client'; import {useEffect,useState} from 'react'; import Shell from '@/components/Shell'; import {api} from '@/lib/api';
export default function Staff(){const [rows,setRows]=useState<any[]>([]);useEffect(()=>{api('/staff').then(setRows)},[]);return <Shell title="Personal"><div className="section-title"><div><h2>Docentes, administrativos y obreros</h2><div className="muted">Cargos, títulos profesionales, datos laborales y bancarios.</div></div></div><div className="table-wrap"><table><thead><tr><th>Cédula</th><th>Nombre</th><th>Cargo</th><th>Función</th><th>Ingreso MPPE</th></tr></thead><tbody>{rows.map(s=><tr key={s.id}><td>{s.identityNumber}</td><td>{s.lastName}, {s.firstName}</td><td>{s.cargoCode||'—'} · {s.cargoDescription||'—'}</td><td>{s.institutionalFunction||'—'}</td><td>{s.ministryEntryDate?new Date(s.ministryEntryDate).toLocaleDateString('es-VE'):'—'}</td></tr>)}</tbody></table></div></Shell>}
