'use client'; import {useEffect,useState} from 'react'; import Shell from '@/components/Shell'; import {api} from '@/lib/api';
export default function Enrollments(){const [rows,setRows]=useState<any[]>([]);useEffect(()=>{api('/enrollments').then(setRows)},[]);return <Shell title="Matrícula"><div className="section-title"><div><h2>Nómina anual</h2><div className="muted">El número de lista permanece fijo aunque el estudiante sea retirado.</div></div></div><div className="table-wrap"><table><thead><tr><th>Año</th><th>N°</th><th>Cédula</th><th>Estudiante</th><th>Grado</th><th>Sección</th><th>Condición</th></tr></thead><tbody>{rows.map(e=><tr key={e.id}><td>{e.academicYear?.name}</td><td>{e.listNumber||'—'}</td><td>{e.student.identityNumber||'—'}</td><td>{e.student.lastName}, {e.student.firstName}</td><td>{e.gradeLevel}°</td><td>{e.section.name}</td><td><span className="badge">{e.condition}</span></td></tr>)}</tbody></table></div></Shell>}
