'use client'; import {FormEvent,useState} from 'react'; import {useRouter} from 'next/navigation'; import {API} from '@/lib/api'; import {BRANDING} from '@/lib/branding';
export default function Login(){const [error,setError]=useState('');const router=useRouter();async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();setError('');const f=new FormData(e.currentTarget);const r=await fetch(`${API}/auth/login`,{method:'POST',headers:{'Content-Type':'application/json'},credentials:'include',body:JSON.stringify({email:f.get('email'),password:f.get('password')})});if(!r.ok){setError('Usuario o contraseña inválidos');return}const j=await r.json();localStorage.setItem('sige_access',j.accessToken);router.push('/dashboard')}return <div className="login"><div className="login-hero"><img src={BRANDING.schoolLogo} alt="Escudo ET Isaías Medina Angarita" style={{width:90,height:101,objectFit:'contain'}}/><div className="hero-title">Sistema Integral de Gestión Escolar</div><p style={{maxWidth:560,fontSize:18,lineHeight:1.6}}>Matrícula, representantes, planes de estudio, notas, materia pendiente, reportes y constancias en una plataforma segura y trazable.</p></div><div className="login-panel"><form className="form" onSubmit={submit}><h1>Iniciar sesión</h1><p className="muted">Acceso para Dirección, Secretaría y Docentes.</p>{error&&<div className="alert">{error}</div>}<label>Correo</label><input className="input" name="email" type="email" defaultValue="admin@etima.local" required/><label>Contraseña</label><input className="input" name="password" type="password" required/><button className="btn" type="submit">Entrar al sistema</button></form></div></div>}
