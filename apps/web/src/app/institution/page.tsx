'use client'; import {useEffect,useState} from 'react'; import Shell from '@/components/Shell'; import {api} from '@/lib/api';
export default function Institution(){const [i,setI]=useState<any>();useEffect(()=>{api('/administration/institution').then(setI)},[]);return <Shell title="Institución"><div className="card"><div style={{display:'flex',alignItems:'center',gap:20}}><img src="/brand/escudo.png" style={{width:90}}/><div><div className="muted">Epónimo actual</div><h1>{i?.name||'ET Isaías Medina Angarita'}</h1><p>Código de Plantel: {i?.plantCode||'—'} · Dependencia: {i?.dependencyCode||'—'} · Estadístico: {i?.statisticalCode||'—'}</p></div></div></div><div className="section-title"><h2>Director actual</h2></div><div className="card"><b>{i?.directorTitle} {i?.directorName||'Por configurar'}</b><p>{i?.directorIdentity||''}</p><p className="muted">{i?.directorPhone} · {i?.directorEmail}</p></div></Shell>}
