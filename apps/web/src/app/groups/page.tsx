'use client'; import {useEffect,useState} from 'react'; import Shell from '@/components/Shell'; import {api} from '@/lib/api';
export default function Groups(){const [rows,setRows]=useState<any[]>([]);useEffect(()=>{api('/groups').then(setRows)},[]);return <Shell title="Grupos de participación"><div className="section-title"><div><h2>Grupos estables</h2><div className="muted">Inscripción independiente, compartida entre diferentes grados y secciones de Media General.</div></div></div><div className="grid">{rows.map(g=><div className="card" key={g.id}><h3>{g.name}</h3><div className="metric">{g.memberships.length}</div><div className="muted">participantes</div></div>)}</div></Shell>}
