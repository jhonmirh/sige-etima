'use client';
import Link from 'next/link'; import { useEffect,useState } from 'react'; import { BRANDING } from '@/lib/branding'; import { LayoutDashboard,Users,GraduationCap,ClipboardList,BookOpen,UserRoundCog,ChartNoAxesCombined,Building2,LogOut,SunMoon,Shapes,ContactRound } from 'lucide-react'; import { useRouter } from 'next/navigation';
const nav=[['/dashboard','Panel',LayoutDashboard],['/students','Estudiantes',Users],['/representatives','Representantes',ContactRound],['/enrollments','Matrícula',ClipboardList],['/grades','Notas',GraduationCap],['/plans','Planes de estudio',BookOpen],['/groups','Grupos',Shapes],['/staff','Personal',UserRoundCog],['/reports','Reportes',ChartNoAxesCombined],['/institution','Institución',Building2]] as const;
export default function Shell({children,title}:{children:React.ReactNode,title:string}){const router=useRouter();const [dark,setDark]=useState(false);useEffect(()=>{const d=localStorage.getItem('theme')==='dark';setDark(d);document.documentElement.dataset.theme=d?'dark':'light'},[]);function theme(){const d=!dark;setDark(d);localStorage.setItem('theme',d?'dark':'light');document.documentElement.dataset.theme=d?'dark':'light'}function logout(){localStorage.removeItem('sige_access');router.push('/login')}return <div className="shell"><aside className="sidebar"><div className="brand"><img src={BRANDING.schoolLogo} alt="Escudo ET Isaías Medina Angarita"/><span><strong>{BRANDING.systemName}</strong><br/><small>{BRANDING.systemSubtitle}</small></span></div><nav className="nav">{nav.map(([href,label,Icon])=><Link key={href} href={href}><Icon size={18}/> <span>{label}</span></Link>)}</nav></aside><section className="content"><header className="topbar"><div><strong>{title}</strong><div className="muted">{BRANDING.schoolName}</div></div><div style={{display:'flex',gap:8}}><button className="btn secondary" onClick={theme}><SunMoon size={17}/> Tema</button><button className="btn secondary" onClick={logout}><LogOut size={17}/></button></div></header><main className="main">{children}</main></section></div>}
